common:
  storage: local
  storage_path: /var/lib/docker/registry
  secret_key: _env:SECRET_KEY:supersecret
  search_backend: sqlalchemy
  sqlalchemy_index_database: sqlite:////var/lib/docker/registry/index.db

prod:
  {}
